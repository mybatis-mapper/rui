package ${package};

import ${package.parent}.model.${it.name.className};
import tk.mybatis.mapper.common.Mapper;

/**
* ${it.comment}
*/
public interface ${it.name.className}Mapper extends Mapper<${it.name.className}> {

}
